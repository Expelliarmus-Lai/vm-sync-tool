# 更新日志

语言：[中文](CHANGELOG.md) | [English](CHANGELOG.en.md)

本文件记录正式发行版本的主要变化。完整实现细节和逐次提交可在 Git 历史中查看。

## v1.3.0 - 2026-07-23

### 新增

- 新增命名同步配置。每条配置使用稳定 UUID，保存共享 VMX/虚拟机凭据以及两个项目槽位。
- 在主界面提供配置的新建、立即切换、保存、重命名和删除操作；新建配置可复制当前内容或创建空白配置。
- 配置下拉列表最多显示 8 条记录，更多配置通过滚动条查看。
- 新增共享 `vmrun_output.py` 解码模块，统一处理 VMware 和虚拟机 PowerShell 返回的 UTF-8、UTF-16、Windows 本地代码页与 GB18030 输出。

### 改进

- 配置写入改为同目录临时文件加原子替换，并保留上一份有效文件为 `config.json.bak`。
- 主配置损坏时保留为 `config.json.corrupt`，并在可能时从备份恢复；配置保存失败会回滚内存状态并在界面提示。
- 配置切换会刷新共享 VM、项目输入栏、状态检查、日志和同步管理器的缓存状态。
- 重命名编辑移动到主窗口配置栏，改善 Windows 中文输入法的候选框、焦点和字号表现。
- 配置下拉栏会在点击软件其他区域、应用失焦、窗口最小化或隐藏到托盘时关闭。
- 修正高 DPI 下下拉栏宽度重复缩放、选项高度不足、文字裁切、边框覆盖和圆角锯齿问题。
- Windows 下拉浮层使用 DWM 原生抗锯齿圆角；保留选中项和鼠标悬停的灰色反馈。
- 新建配置弹窗根据实际内容调整尺寸，明确显示“创建配置”和“取消”按钮。
- VM 状态后台检查增加配置版本校验，避免旧 VMX 的迟到结果覆盖当前状态。

### 修复

- 修复通过 `CTkFrame.bind_all` 注册全局鼠标事件导致源码模式启动崩溃的问题，改为通过根窗口注册。
- 修复软件隐藏后配置下拉栏仍留在屏幕上并可点击的问题。
- 修复未完成重命名在退出时静默丢失的问题。
- 修复虚拟机中文路径或 PowerShell 错误信息乱码的问题。

### 验证

- 256 项自动化测试通过。
- 完整 `py_compile` 检查通过。
- PyInstaller 文件夹版发行构建和 zip 内容检查通过。

## v1.2.1 - 2026-06-02

### 改进与修复

- 加强双项目启动、暂停、全量同步和虚拟机关闭时的状态收尾。
- 使用运行令牌阻止暂停或重启后的迟到增量覆盖、`.bin` 回传、日志和通知。
- 单个项目增量上传超时时只暂停该项目，并在顶部显示“部分异常”，另一个项目继续运行。
- 优化 `.bin` 启动基线、首次同内容时间更新回传和复制后的时间漂移抑制。
- 改善托盘状态、项目固件就绪时间和虚拟机停止后的界面反馈。
- 更新双项目用户文档、配置模板和回归测试。

## v1.2.0 - 2026-05-29

### 新增

- 支持同一个虚拟机下最多两个独立 Keil 同步项目。
- 两个项目拥有独立的路径、启动/暂停、保存检测、全量同步/取消、日志、上传队列、hash 基线和 `.bin` 回传目录。
- 新增共享虚拟机配置区域以及“添加同步项目/停用项目 2”布局。
- 顶部“启动全部”采用原子预检：任一项目失败时所有项目保持停止。

### 改进

- 配置格式迁移为共享 VM 字段加 `projects` 列表，并自动迁移旧版单项目配置。
- 文件复制、创建、删除和覆盖类 `vmrun` 操作串行化，降低 VMware VIX 并发不稳定风险。
- 双项目模式窗口扩展到 `1180x955`，单项目模式保持 `700x955`。

## v1.1.0 - 2026-05-28

- 新增中文/英文运行时语言切换和系统语言自动检测。
- 完善双语 README、用户指南和发行包文档。
- 改进系统托盘菜单、窗口图标和打包流程。

## v1.0.0 - 2026-05-27

- 首个正式版本。
- 支持 `vmrun.exe` 自动探测、预检、增量同步、全量同步和 `.bin` 固件回传。
- 支持系统托盘运行和安全退出清理。
